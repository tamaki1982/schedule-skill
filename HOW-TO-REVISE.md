# 生成した後の直し方

出来上がったスケジュールを直す方法は3つあります。**直したい内容の大きさで選びます。**

---

## 1. 3つの手段

| 手段 | どこで直すか | 結果が残るか | 向いていること |
|---|---|---|---|
| **AI に言う** | AI との会話 | **残る。** 新しい版の HTML と JSON が出る | 日程の変更、作業の追加・削除、ステータスの更新、体制の変更。**波及するものすべて** |
| **画面で直す** | 出来上がった HTML の上 | **その場では残らない。**「書き出す」を押すと新しいファイルになる | 動かしてみて見え方を確かめる。1〜2か所の手直し |
| **設定ファイルを直接書く** | 置き場の `output/` の中の `.json` をエディタで開く | **残る。** 版は上がらない | 誤字の訂正など、1か所で閉じる直し |

### 迷ったときの目安

| 直したい内容 | どれを使うか |
|---|---|
| 動かしたら全体がどう変わるか、まず見たい | 画面で直す |
| 直したい場所が2か所以上に波及する | AI に言う |
| 1か所で閉じる。日程に響かない | 設定ファイルを直接書く |
| どれか分からない | **AI に言う。** 波及する範囲の判断ごと任せられる |

**日程に響く直しは AI に言ってください。** タスクを1本ずらすと、後続の作業、マイルストンの期日、
予備日の残量、全体の終了日までが連動します。手で直すとそこが合わなくなります。

---

## 2. AI への言い方

**普通の言葉で構いません。** どのファイルを直すか、どの版を起点にするかは、AI 側が判断します。
対象のスケジュールが複数ある場合だけ、どれの話かを添えてください。

### 日程をずらす

```
実装工程を2週間後ろ倒しして
```
```
機能Bの着手を 10/20 からにして
```
```
テストを3日短くして
```

「延ばして」「倍にして」は、**開始日から終了日までの長さ**を動かします。
作業量そのものが増えたのか、待ちが入って後ろへ動いただけなのかは、判断が付かなければ確認されます。

### 作業を足す・消す

```
検証工程にセキュリティ確認の作業を足して
```
```
リハーサルは不要になったので消して
```
```
機能Cを追加したい。実装と動作テストを入れて
```

足した作業の日数は、その分野の標準的な見積もりから置かれます。
**足した結果、全体の終了日が動いた場合は必ず報告されます。** 黙って後ろへ動くことはありません。

### 担当を変える

```
画面側実装の担当を、設計担当に変えて
```
```
機能A 完成の到達責任を検証担当にして
```

**担当は役割名で持っています。個人名は入りません。**
「設計」「実装」「検証」のような役割の名前で伝えてください。
どんな役割があるかは、そのスケジュールの中に一覧として入っています。

### ステータスを変える（完了にする、着手した）

```
設計工程は全部終わった
```
```
機能Aの実装に入った
```
```
要件調査は完了。いま画面設計をやっている
```

**ステータスの更新は、いちばん軽い依頼です。** 日程には触らず、その作業のステータスだけが変わります。
完了にすると、作業名に打ち消し線が入り、バーの塗りが変わります。
**その作業が属するマイルストン・工程・フェーズも、配下が全部完了していれば自動で完了になります。**

**タスクだけは、画面のタスク詳細からも直接切り替えられます。**「ステータス」のプルダウンで
未着手・進行中・完了を選べます。ただし画面での切り替えは、書き出さないとファイルには残りません
（残したいなら AI に伝えるほうが確実）。マイルストン以上は配下から決まるため、画面での切り替えは
できません。

### 期限に間に合わせる

```
公開を2週間前倒ししたい。間に合わせる方法を出して
```
```
12/11 が動かせなくなった。どこを詰めれば入るか
```

**間に合う計画が出ます。** ただし、そのために何が必要か（どの作業を並行にしたか、どの役割を何名にしたか）が
必ず添えられます。**そこを読まずに「いまの体制で間に合う」と受け取らないでください。**

並行にする・人を増やすでも届かない場合は、**何日足りないかがそのまま出ます。**
休日出勤で辻褄を合わせることはしません。

### 体制が変わった

```
実装を2名から3名にして引き直して
```
```
検証の担当が抜けた。1名前提で組み直して
```

人数が変わると所要日数が計算し直され、**全体の日程が大きく動きます。**
この直しだけは、日程がまとめて変わることを前提にしてください。

---

## 3. 言ったあと何が起きるか

| 順 | 起きること |
|---|---|
| 1 | 一番新しい版のデータを起点に、指示された箇所だけが直される |
| 2 | 中身に矛盾が無いか確認される。通らなければ、**ファイルは1つも出ずに止まる** |
| 3 | **新しい版のファイルが2つできる。** `.html` と `.json` の対 |
| 4 | 変更履歴に1行増える |
| 5 | 何を変えたか、全体の終了日が動いたかが報告される |

**出る場所とファイル名（置き場の中）:**

```
<置き場>/output/<識別子>/
  <識別子>_r01_2026-09-07.json     最初に作ったもの
  <識別子>_r01_2026-09-07.html
  <識別子>_r02_2026-09-12.json     直した後
  <識別子>_r02_2026-09-12.html
  CHANGELOG.md                     版ごとの1行履歴
```

- **`r` の後ろの数字が版です。** 直すたびに1つ増えます。
- **前の版は消えません。** 戻したいときは、古いほうの HTML を開けば元の内容が見られます。
- **開いていた古い HTML は変わりません。** 新しいほうを開き直してください。

---

## 4. 画面で直したものを、AI に引き継ぐ

画面で直した内容は、**そのままでは残りません。** 再読み込みすると元に戻ります。
残したいときは、次の流れになります。

1. 画面の上で直す。
2. **「書き出す」を押す。** `.html` と `.json` が2つ、ブラウザのダウンロード場所に出ます。
3. **その2つを、置き場の `output/<識別子>/` へ移します。**
4. 次に AI に直してもらうときは、そのまま普通に言えば大丈夫です。

**3 は AI に頼めます。**「書き出したファイルを取り込んで」と伝えると、正しい場所へ移されます。

**AI は、その識別子のフォルダの中で版が一番新しいものを起点にします。**
書き出したファイルをダウンロード場所に置いたままにすると、AI はそれを見つけられず、
**画面で直す前の内容から作業を始めてしまいます。** 移すか、置いてある場所を伝えるか、どちらかが必要です。

---

## 5. 画面でできること・できないこと

### 画面で直せるもの

| 種類 | 内容 |
|---|---|
| 日程 | バーを左右にドラッグして動かす。端をつかんで伸び縮みさせる。マイルストンの旗を動かす |
| 構成 | 工程・マイルストン・作業を足す、消す、並べ替える、所属を付け替える |
| 中身 | 名前、開始日と終了日、メモ、達成条件/成果物、タスクのステータス |
| 形 | フェーズの組み方（工程を挟む形と挟まない形）の切り替え |
| 暦 | 休みの日にも日程を置けるようにする設定 |
| データ | 設定ファイルのビューで、表示中のフェーズの中身を直接書き換える |

### 画面ではできないもの

| できないこと | 代わりに |
|---|---|
| **マイルストン以上のステータスを変える** | **AI に「設計は終わった」と言う。** 配下のタスクがすべて完了すれば自動で完了になります（タスク自体は詳細パネルの「ステータス」プルダウンから画面でも切り替えられます） |
| **色を変える（バーの色、休みの日の色）** | **AI に「バーの色を青にして」と言う。** 色は作ったときのまま固定です |
| **判断基準（`definitions/` の中身）を直す** | **AI に「今後はこう作って」と言う。** 直すべき1ファイルを選び、置き場側に写して直します。画面からは触れません |

**色を画面から外したのは、「直したのに、開き直すと戻っている」が起きるためです。**
タスクのステータスは画面からも切り替えられますが、開き直すと元に戻る点は同じです。
残る形にするには、切り替えたあとに書き出すか、AI に言うのが確実です。

### 画面で直しても、ファイルには残らない

画面はいつでも直せますが、**それは試すための場所です。**
直した内容は、「書き出す」を押すまでファイルには残りません。
再読み込みやタブを閉じる操作をすると、確認を挟んだうえで消えます。
