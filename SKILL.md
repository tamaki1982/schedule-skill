---
name: schedule-gantt
description: 依頼者の入力文からプロジェクトのスケジュールを組み立て、単体で開けるHTMLのガントチャートとJSONを出力するスキル。「スケジュール作って」「予定を引いて」「〇〇のスケジュールを作成して」「工程表を作って」といった新規依頼、既存スケジュールの修正依頼（「実装を2週間後ろ倒しして」等）、このツール自体の仕様についての質問（保存の仕組み・印刷・画面の操作範囲など）で使う。
allowed-tools: Bash(python3 *), AskUserQuestion
---

# スケジュール生成スキル

このファイルは、「スケジュール作って」「予定を引いて」「〇〇のスケジュールを作成して」といった
依頼を受けたときに読む手順書である。**手順だけを書いてある。**

判断の理由や、なぜこの形にしたかは `SPEC.md` にまとめてあるので、そちらは読まなくても
このファイルだけで最初から最後まで実行できる（`SPEC.md` は同梱していないことがある。その場合は
参照できない旨を利用者に伝える）。ただし、入力が本書の想定から外れていて
判断に迷った場合は、`SPEC.md` の該当章（各 Step の末尾に番号で示す）を確認する。

## この文書の位置づけ

生成のたびに読まれるので、経緯を書かず手順だけに絞ってある（経緯は `SPEC.md`、約5,000行）。
生成の基準（階層の定義、日程の原則、標準作業、見積もり）は `definitions/` が正で、ここでは重複させない。
ただし、判断を誤ると出力が壊れるものには、迷わないための短い注記を1行だけ添える。

### この文書で使う2つの場所の呼び名

このスキルは `git clone` でそのまま配置される配布物であり、置き場所は利用者の環境次第で変わりうるため、
「スキル本体の場所」と「生成物を置く場所」を区別する。決め方は本書 1-0 にある。

| 呼び方 | 何を指すか |
|---|---|
| **SKILL_ROOT** | このスキル一式（この `SKILL.md` や `templates/` がある場所。通常は `~/.claude/skills/schedule-gantt/`）。clone 先を変えれば物理的な場所も変わる |
| **置き場** | 利用者が選んだ、生成物と自分用の定義ファイルを置く場所。SKILL_ROOT とは別の場所にあり、スキル本体の更新・再インストールの影響を受けない |

**以降、本書で単に `schema/` `templates/` と書いた場合は SKILL_ROOT 配下を指す。単に `output/` と
書いた場合は置き場配下（`<置き場>/output/`）を指す。`definitions/` だけは例外で、置き場に
`definitions/` があればそちらを読み、無ければ SKILL_ROOT の `definitions/`（配布時点の既定値）を読む
—— この優先順位は毎回書かない。**

---

## 0. いつ動くか

「スケジュール作って」「予定を引いて」「〇〇のスケジュールを作成して」「工程表を作って」
といった依頼を受けたとき、このスキルを使う。

やることは4つのどれかになる。

| 依頼の形 | やること |
|---|---|
| 新しいスケジュールを作ってほしい | 「A. 新規生成」（本書 1〜8） |
| 既存のスケジュールを直してほしい（チャットで指示） | 「B. AI への修正指示」（本書 9） |
| 画面や JSON ファイルの直し方を知りたい | 「C. その他の直し方」（本書 10）を案内する |
| **このツール自体について質問された**（保存はどうなる、印刷はできるか、なぜ1フェーズしか出ないのか、など） | 「D. 仕様についての質問」（本書 11） |

---

## A. 新規生成

### Step 1. 前提の読み込み

#### Step 1-0. 場所を決める（SKILL_ROOT と 置き場）

**置き場が既に確定していて実在も確認できるなら、2周目以降はこの Step をスキップしてよい。**

1. **SKILL_ROOT を決める。**
   - いま読んでいるこの `SKILL.md` を読み込んだ際の絶対パス（例: `~/.claude/skills/schedule-gantt/SKILL.md`）
     から、その親ディレクトリを SKILL_ROOT とする（`SKILL.md` と `templates/` が同じ階層にある場所）。
   - **環境変数（`${CLAUDE_SKILL_DIR}` 等）には頼らない。** このスキルは `git clone` でそのまま
     配置される配布物であり、そうした変数が用意されない環境でも動く必要があるため、常に
     「読み込んだファイル自身の絶対パス」から解決する。

2. **置き場を決める。**
   - 作業対象のプロジェクトに `CLAUDE.md` があれば、その中に見出し
     `## schedule スキルの生成物置き場所` を探す。
   - 無ければ、利用者のグローバル `CLAUDE.md`（例: `~/.claude/CLAUDE.md`）の中に同じ見出しを探す。
   - **見つかり、かつそのパスが実在するディレクトリなら、それを置き場とする。** Step 1-1 へ進む。
   - **見つからない場合は、利用者に聞く。** 候補を1つ提示して「ここでよいか」を確認する形にする
     （パスを考えさせない）。候補は「いま作業しているプロジェクト直下の `schedule/`」でよい。
     例:「生成したファイルと自分用の設定ファイルは `<カレントディレクトリ>/schedule/` に置きます。
     よろしいですか？別の場所がよければ教えてください」
   - **記録はあるがパスが見つからない場合**（フォルダが移動・削除された等）は、その旨を伝えたうえで
     同じやり方で聞き直し、新しい場所に作り直す。
   - 場所が決まったら、次をこの順で行う。
     a. 置き場のディレクトリを作る（無ければ作成する）。
     b. SKILL_ROOT の `definitions/` 一式を、置き場の `definitions/` へコピーする。
        **利用者が自分用に書き換えるのはこのコピーの方。** 以後、生成・改版のたびに
        「置き場の `definitions/` があればそちらを読み、無ければ SKILL_ROOT の既定値を読む」を適用する。
     c. 決めた場所を、見出し `## schedule スキルの生成物置き場所` として、探した先の `CLAUDE.md`
        （プロジェクトのものが見つからずグローバルを使った場合はグローバルの方）に1行で記録する。
        次回以降は 2 の冒頭に戻ってこの記録を読むだけで済み、聞き直さない。

   **SKILL_ROOT の中（definitions/ の既定値を含む）には、生成物・置き場の記録・利用者用にコピーした
   definitions/ のいずれも書き込まない。** スキルの更新・再インストールで失われる可能性がある場所だからである。

#### Step 1-1. 定義ファイルの読み込み

1. `definitions/00-index.md` を読む（定義バージョン・プリセット一覧・読み込み順を確認）。
2. `definitions/10-hierarchy.md` / `20-scheduling-rules.md` / `30-output-rules.md` をすべて読む。
3. **`definitions/50-user-context.md`（利用者の前提）を読む。** その組織のやり方が書いてある層で、
   **プリセットより優先する。** 既定のままのこともあるが、必ず読む。
4. `schema/schedule.schema.json`（SKILL_ROOT 配下）を読む。
5. `templates/schedule.template.html` と `templates/build.py`（いずれも SKILL_ROOT 配下）が
   存在することを確認する。
   **無ければここで止め、テンプレート／ビルド処理が未整備であることを伝える。代わりの HTML を書かない。**

プリセットはこの時点では読まない（Step 2 で決めてから読む）。

### Step 2. 入力の解釈とヒアリング

#### Step 2-0. 入力文からの抽出

入力文から次を抽出する。取れないものは「未取得」として Step 2-1 に進める。

| 抽出項目 | 抽出できない場合 |
|---|---|
| 用途（開発 / イベント / 受託 / 制作 など） | Step 2-1 のヒアリング対象候補にする |
| 取り組みの数 | 1つとして扱う |
| 対象（何について） | 依頼文の主語から推定 |
| やること | 必須。取れなければ Step 2-1 で最優先に聞く |
| 期限・目標日（「いつまでに」） | Step 2-1 のヒアリング対象候補にする |
| **着手時期（「いつから」）** | **Step 2-1 のヒアリング対象候補にする** |
| 規模の手がかり（体制） | Step 2-1 のヒアリング対象候補にする |
| 明示された工程 | 標準構成どおり |
| 既に終わっている作業 | すべて未着手として扱う（既完了と書かれた場合のみ `done`） |
| 除外の指定 | 除外なし |

**「いつまでに」と「いつから」は別物として拾う。** 「年内に公開したい」は目標日、
「来春から取りかかる」は着手時期。**両方あることも、片方だけのことも、どちらも無いこともある。**
言い回しの読み方（「年内」「今期中」「来春」「四半期」など）は `20-scheduling-rules.md` 第3章 Step 1 の表にある。
**入力文だけで確信を持って読み取れた項目は、Step 2-1 のヒアリング対象に入れない**（聞き直さない）。

#### Step 2-1. ヒアリング（AskUserQuestion を使う）

**文章で複数の質問を並べない。** 選択肢を提示する質問ツール（AskUserQuestion）を使い、
**1回の呼び出しでまとめて聞く。** ヒアリングは原則1回で終える。

1. **対象を絞る。** Step 2-0 で「未取得」になった項目のうち、次を優先順位順に**最大4件**まで選ぶ。
   それ未満ならその数だけでよい。

   | 優先順位 | 項目 | 聞く条件 |
   |---|---|---|
   | 1 | やること | 依頼文から特定できないとき（最優先。これが無いと何も組めない） |
   | 2 | 着手時期（いつから） | 入力文に手がかりが無いとき |
   | 3 | 目標日・期限（いつまでに） | 入力文に手がかりが無いとき |
   | 4 | 用途・プリセット | `definitions/presets/` のどれに合うか自信が持てないとき |
   | 4 | 体制の規模 | 人数の手がかりが依頼文に一切無いとき |

   **4は同着。** 両方とも未取得ならどちらか一方（依頼の内容から見て影響が大きい方）を選ぶ。
   **対象が0件になった場合は、ヒアリングをスキップして Step 2-2 へ進む。**推定した内容は
   後で必ず `meta.assumptions` に書く。

2. **1回の AskUserQuestion 呼び出しで、対象すべてを一度に聞く。** 質問ごとに選択肢を用意する。
   - **選択肢の先頭は「推奨」にし、なぜそれを勧めるかが1行で分かるようにする。**
     入力文に手がかりがあれば、それをそのまま推奨選択肢にする（例: 「11月中」という記載があれば
     「11月最終稼働日（記載から読み取り）」を先頭に）。
   - **利用者が選択肢に無い答えを書ける前提で作る。** 用意した選択肢に無理に押し込めない
     （自由記述はツール側の機能に任せる）。
   - 質問文と選択肢の作り方の例:

     | 質問 | 選択肢の例（先頭が推奨） |
     |---|---|
     | いつから着手しますか | すぐ着手（今日から）／来週から／来月から／未定（逆算で決めてほしい） |
     | いつまでに仕上げますか | 入力文から読み取った日（例:「11月中」→11月最終稼働日）／指定なし（着手時期を起点に前向きに積む） |
     | どの分野に近いですか | `definitions/presets/` から関連度の高い順に2〜3件を提示／その他（自動推定に任せる） |
     | 体制の規模は | 1名／2〜3名／4名以上／未定（プリセットの既定値を使う） |

3. **回答が返ったら、それ以上聞き返さない。** ヒアリング対象に入れなかった項目・回答が無かった
   細部は推定で埋め、推定した内容と根拠を `meta.assumptions` に必ず書く。

#### Step 2-2. プリセットの決定

プリセットの決め方（`definitions/00-index.md` の「どのプリセットを使うか」と同じ）:

1. 入力文でプリセットが名指しされていれば、それを使う。
2. 名指しがなければ、**`definitions/presets/` 直下のフォルダを見て**（`_` で始まるものを除く）、
   各フォルダの `00-preset.md` の「こういう依頼のときに使う」を照合する。
   **索引の一覧に載っていないフォルダも選択肢に入れる。**
3. どれにも当てはまらなければ、最も近いものを選び、選んだ理由と当てはまらなかった点を
   `meta.assumptions` に記録する。

決めたプリセットの6ファイル（`00-preset.md` 〜 `50-domain-notes.md`）をすべて読む。

**用途が2つにまたがる場合**は、プロジェクトを分けるのではなく、ドキュメント（HTML）を
2本に分けてよいか利用者に確認する（1つのドキュメントで使うプリセットは1つに限るため）。

### Step 3. 骨格の決定

1. **プロジェクトの区切りを決める。** 既定は1件。分けるのは次のどれかに当てはまるときだけ。
   - 依頼の中に、始まりと終わりが別の取り組みが2つ以上ある
   - 相手が別
   - 目的が別で、片方をやめても他方が成立する

   同じ取り組みの中の段階は「フェーズ」に、同じ取り組みの中の対象物は「工程を挟まないフェーズの
   マイルストン」にする。プロジェクトを分けたかどうかと理由は、後で必ず `meta.assumptions` に書く。

2. プリセットの `20-structure.md` の判定基準で規模を判定する。

3. **プロジェクトごとにフェーズの区切りを決める。**

4. **フェーズごとに構成の形を決める**（フェーズ単位で独立に判断してよい。1本の中で混在してよい）。

   | そのフェーズの中身 | 形 |
   |---|---|
   | 進め方の段階で分けたい（設計→実装→検証のように、前が終わらないと次に進めない） | 工程を挟む（`stages[]`） |
   | 機能や対象物で括りたい／同じ手順の繰り返し（第1回・第2回…） | 工程を挟まない（`milestones[]` を直下に） |

   迷ったら、工程を挟む形で組んでみて、同じ工程名がそのフェーズの中で2回以上出てくるなら
   挟まない形に切り替える。選んだ形と理由は `meta.assumptions` に書く。
   **`stages` と `milestones` を同じフェーズに両方置いてはならない。**

5. 工程を挟む形は標準構成から工程とマイルストンを取り出す。挟まない形は対象物ごとにマイルストンを1つ置く。
6. 入力文で明示された工程を追加し、除外指定された工程を落とす。
7. 各マイルストンに、標準構成で指定された作業を並べる（作業の型は `10-standard-tasks.md`）。
8. `50-domain-notes.md` 第1章・第3章を確認し、縮められない待ち時間・見落としやすい作業を
   該当があればタスクとして足す。
9. **タスク名を、それだけで意味が通る形にする。**
   - 標準作業名をそのまま使わず対象を足す（例:「要件調査」→「バーチャルオフィス 要件調査」）
   - 工程を挟まない形では対象物を名前の先頭に置く
   - 工程を挟む形では工程名が上に出ているので対象を繰り返さない
   - 予備日は予備日と分かる名前にする（例:「実装バッファ」）
   - プロジェクト名はタスク名に含めない

**1フェーズの目安は40行程度（工程行+マイルストン行+タスク行）。** 超える場合は
フェーズを割る → 工程を挟むのをやめる → タスクの粒度を見直す、の順で検討する。

### Step 4. 日程の算出

1. `20-scheduling-rules.md` 第1章に従い、タイムライン範囲の休業日を `calendar.holidays` に展開する。
   **年をまたぐ場合は、その年の休業日が一覧にあるかを先に確かめる。** 無ければ一覧に追記してから計算する。
   `50-user-context.md` 第2章に組織固有の休業日があれば、それも足す。
   **依頼文に休日の作業日が明示されている場合だけ**（「イベント本番は 11/22（日）」など）、
   その日を `calendar.exceptionalWorkingDays` に入れ、理由を `meta.assumptions` に1件残す。
   **根拠が無ければ入れない。目標日に間に合わせるために休日を使わない**（`20-scheduling-rules.md` 第1章）。
2. `30-estimation.md` で各タスクの `effortDays` を決める。
   ```
   effortDays = 標準工数 × 規模倍率 × 条件倍率（掛け合わせ） × 繰り返しの係数
   ```
   **倍率はすべて工数に掛ける。所要日数には掛けない。** 同じ標準作業が1フェーズに2回以上出る場合の
   係数は `30-estimation.md` の「同じ作業が繰り返し出るとき」にある。
3. `40-team.md` の値で `durationDays` を算出する。**切り上げは最後に1回だけ。**
   ```
   有効人数   = 1 + (人数 − 1) × 0.7
   durationDays = ceil( effortDays / (有効人数 × 実働係数) )
   ```
   `10-standard-tasks.md` で「そのまま効く」とされた作業は有効人数ではなく人数をそのまま使い、
   「効かない」とされた作業には人数を掛けない。**結果が15稼働日を超えたら分割する**
   （工数を4：6で分け、直列に置く。`20-scheduling-rules.md` 第2章）。
4. `20-scheduling-rules.md` 第3章に従って日程を置く。
   - **目標日だけ** → 目標日から後ろ向きに積む
   - **着手時期だけ / 両方** → 着手時期を起点に前向きに積む
   - **どちらも無い** → 「今日」の翌稼働日から前向きに積む
   - **目標日を超えたら、第3章 Step 5 の順（並行化 → 増員 → 超過）で詰める。**
     **目標日に間に合う計画を出し、そのために何が要るかを前提とリスクに必ず書く。**
   **プロジェクトが複数あれば、プロジェクトごとに独立して行う。**
5. `30-estimation.md` の並行してよい組み合わせに従い、並行できるタスクを `SS` + `lagDays` で重ねる。
   **同じ役割どうしは、並行してよい組み合わせでも重ねない。**
6. `30-estimation.md` のバッファ量に従い、予備日タスクを配置する
   （`isBuffer: true` / `effortDays: 0` / 担当役割なし）。
7. 開始日・終了日を稼働日に寄せる（開始日は直後の稼働日、終了日は直前の稼働日）。
8. 同一役割のタスクが、**同じプロジェクトの中で**重なっていないか確認する。重なっていたら直列に
   並べ直す（プロジェクトをまたいだ重なりは直さず、Step 5 の7でリスクに残す）。
9. 各マイルストンの `date` を、配下タスクの最遅終了日に置く。
10. クリティカルパスをプロジェクトごとに算出し、該当タスクの `isCriticalPath` を `true` にする。
11. プロジェクト・フェーズ・工程の `startDate` / `endDate` を、配下から算出した値と一致させて書く
    （これは想定の宣言であり、描画には使われない）。

### Step 5. JSON の組み立て

1. `meta` を埋める。`schemaVersion` は現行のスキーマ版（`schema/schedule.schema.json` の値）、
   `revision` は1、`generatedAt` は現在時刻、`sourceRequest` は入力文の原文、
   `previousRevisionFile` は `null`、`definitionsVersion` は `00-index.md` の値、
   `preset` は採用したプリセットの id / 表示名 / 版数。**`meta.subtitle` は書かない**。
   `meta.notes` は0〜3件で、基準は `30-output-rules.md` 第8章にある（**迷ったら書かない**）。

2. **`meta.groupName` と `meta.title` を決める。**
   - プロジェクトが1件: `groupName` は書かない。`title` = `projects[0].name` + `" スケジュール"`
   - プロジェクトが2件以上: `groupName` を決める（依頼文にまとめて指す言葉があればそれを使い、
     無ければ共通する対象・目的から作る。**中の1つのプロジェクト名を使わない**）。
     `title` = `groupName` + `" スケジュール"`
   - 区切りは半角スペース1つ、接尾語は「スケジュール」で固定。**表題は組み立てるだけで、考えて書かない。**
   - 製品名と取り組み名が両方ある場合は、取り組みの方をプロジェクト名にする（製品名は
     `project.description` へ）。

3. `meta.assumptions` に、Step 2〜4 で推定した内容をすべて書く。特に次の6つは必ず入れる。
   - プリセットを選んだ理由
   - プロジェクトをいくつに分けたかと、その理由
   - プロジェクト名の決め方（製品名と取り組み名の両方がある場合、どちらを採ったか）
   - **目標日と着手時期の解釈**（例:「年内」を何日と読んだか。「来春から」をいつと読んだか）
   - フェーズごとに選んだ構成の形と、その理由
   - **目標日に間に合わせるために増員や並行化をした場合は、どの役割を何名にしたか（既定は何名か）**

4. `id` を採番する。
   - 形式: `prj-<内容>` / `ph-<内容>` / `st-<内容>` / `ms-<内容>` / `tk-<内容>`（内容は名称の英字化。連番は使わない）
   - **ドキュメント全体で一意にする。プロジェクトをまたいだ重複も認めない。**
   - **`-tmp-` を含む id は使わない**（画面上での追加要素の仮 id として予約されている）。

5. `order` を各親の中で1起点の連番にする（`projects[]` はドキュメント内、`phases[]` はプロジェクト内、以下同様）。

6. `display` を埋める。
   - `defaultPhaseId` を書けば親プロジェクトは自動で決まる（`defaultProjectId` は書かなくてよい）。
   - 主題がプロジェクトまでしか決まらない場合は `defaultProjectId` だけを書く。
   - **迷ったらどちらも書かない**（自動選択に任せる方が害が小さい）。
   - 両方書く場合は同じプロジェクトを指すこと。書いた場合はその理由を `meta.assumptions` に1行残す。
   - `defaultExpandLevel` は **常に `task`**（開いた直後に全部見えている状態）にする。
     入力文で開き方が明示的に指定された場合にだけ `auto` / `milestone` のどちらかを使う
     （`20-scheduling-rules.md` 第7章）。
   - `barColorKey` / `nonWorkingDayShading` / `allowNonWorkingDayPlacement` は
     `50-user-context.md` 第8章の値をそのまま書く（依頼文に指定があればそちらを優先）。
     **既定のままなら省略してよい。**

7. `risks` を埋める。`20-scheduling-rules.md` 第9章の共通条件と、プリセットの `50-domain-notes.md`
   第4章の条件の両方を確認する。**プロジェクトが複数あり同じ体制を共有している場合は、その取り合いを
   必ず1件記録する。増員を前提にした場合は、その人数を確保できないと何稼働日届かないかを必ず1件記録する。**

8. `changeLog` に revision 1 のエントリを1件入れる（`instruction` に入力文の原文、`changes` は空配列でよい）。
   `summary` は**その版でやったことだけ**を1〜2文で書く。
   **プロジェクト何件・タスク何件といった構成の数え上げを書かない**（`30-output-rules.md` 第4章）。
   画面は1フェーズ分しか表示しないので、全体の件数が書いてあると、無い物を探させることになる。

9. `resources` にプリセットの `40-team.md` の内容を書き出す（省略しない。画面上で担当役割を
   変える際の選択肢になる）。

10. **状態を書く。** 全タスクの `status` を書く（初版は原則 `notstarted`。入力文で明示的に
    「済んでいる」とされた作業だけ `done` にし、判断を `meta.assumptions` に残す）。
    **プロジェクト・フェーズ・工程・マイルストンの `status` は書かない**（常に配下から算出される）。

11. 最後に、組み立てた `title` と `groupName` が、上の2の規則どおりになっているか見直す
    （プロジェクト名を後から直した場合、表題の組み直しを忘れやすい）。

### Step 6. 検証と出力

1. 組み立てた JSON を、どこか1つのファイルに書き出す（一時ファイルでよい。最終的な置き場所と
   ファイル名は次のコマンドが決める）。**通らずに終わった場合、このファイルが人の手がかりになる。**
2. 次のコマンドを実行する。**SKILL_ROOT と 置き場は、どちらも絶対パスで渡す。**
   ```bash
   python3 "<SKILL_ROOT>/templates/build.py" "<上で書き出したJSONの絶対パス>" --out-root "<置き場>/output"
   ```
   **`--out-root` を省略すると SKILL_ROOT 自身の中の `output/` に書き込もうとしてしまう。**
   そこはスキル本体のフォルダ（更新・再インストールで消える可能性がある場所）なので、必ず置き場を渡す。

   **このスケジュールを作るのに使った定義ファイル（見積もり基準・体制・分野の考え方）は、
   HTML には一切埋め込まれない。** 社外に渡すかどうかを問わず常にこの動きなので、
   フラグの指定は要らない（`30-output-rules.md` 第6章）。
   このコマンドは `templates/validate_schedule.py` で**形（必須項目・型・書式）と不変条件 C1〜C19** を確認し、
   引っかかれば**何も出力せず**日本語のエラー一覧を出して終わる。通れば JSON をテンプレートに埋め込み、
   `<置き場>/output/<meta.id>/<meta.id>_r<版数2桁>_<日付>.json` と同名の `.html` を対で書き出し、
   `<置き場>/output/<meta.id>/CHANGELOG.md` に版の行を追記する。
3. エラーが出た場合は、エラー文が指す場所（プロジェクト／フェーズ／工程／マイルストン／タスクの名前）
   を JSON 側で直し、Step 6 を最初からやり直す。**検証を通らない JSON を出力しない。**

   **やり直しは3回まで。** そして、**やり直す前に前回のエラー一覧と見比べる。**

   | 前回と比べて | どうするか |
   |---|---|
   | エラーの中身が変わった / 減った | 前進している。直して次へ |
   | **中身が前回と同じ** | **直せていない。回数を待たずに止める**（下記） |
   | 3回直しても通らない | **止める**（下記） |

   **同じ箇所で2回続けて落ちたら、こちらの文書が矛盾している可能性を疑う。**
   説明文と、機械が見ている定義（`schema/schedule.schema.json` の `$defs` と
   `templates/validate_schedule.py`）が食い違っていることがある。
   **食い違っていたら、機械が見ているほうが正。** 説明文を勝手に書き換えず、報告に書いて止める。

   **止めるときは、次を伝えて終わる。黙って諦めない。**
   - 何回試したか、最後に残ったエラーの全文
   - **人がどこを直せばよいか**（依頼の内容か、定義ファイルか、こちらの文書の矛盾か）
   - **最後に作った JSON の絶対パス**（消さない。人が直して `build.py` に渡し直せる）
4. **出力先がすでに存在する場合**（同じ `meta.id` の同じ版数のファイルがある場合）、
   `build.py` は上書きせずエラーで止まる。これは新規ではなく改版のはずなので、
   本書「B. AI への修正指示」の手順に切り替える。

### Step 7. 報告

伝える項目は `30-output-rules.md` 第7章の表がすべて。加えて、フェーズごとに選んだ構成の形とその理由を添える。
**Step 6 で通らずに止まった場合は、同章の「検証が通らずに止まった場合の報告」のほうを使う。**

**落としやすいのは次の3つ。**

- **増員を前提にした場合**: どの役割を何名にしたか、その人数を確保できないと何稼働日届かないか。
  タイムライン上は間に合っているので、言わないと「いまの体制で間に合う」と受け取られる
- **増員と並行化でも届かなかった場合**: 何稼働日不足しているか
- **「今日」がタイムラインの範囲外の場合**: その旨（画面に今日の線が出ない）

表題は4番目、一覧はそのすぐ下に置く。次の形でまとめて示す。

```
表題        バーチャルオフィス機能追加 スケジュール
プロジェクト バーチャルオフィス機能追加（1件）
フェーズ    初期開発 / 機能拡張
```

---

## B. AI への修正指示（既存スケジュールを直す）

**利用者向けの案内は `HOW-TO-REVISE.md` にある。** 「どうやって直せばいいか」と聞かれたときは、
この手順書ではなくそちらを案内する。以下は AI 側が実際に差分を適用する手順。

「〇〇を2週間後ろ倒しして」のような、チャットでの修正依頼を受けたときの手順。

### Step 1. 対象の特定

1. 対象のドキュメント識別子（`meta.id`）を特定する。指示に明示がなければ、`output/` 内で
   最後に更新されたものを候補として提示し、確認する。
2. `output/<meta.id>/` 内で `revision` が最大の JSON を読み込む（起点データ）。
3. 定義ファイル（`definitions/` 直下）と `schema/schedule.schema.json` を読み直す。
4. `meta.preset.id` が指すプリセットを読む。
5. プロジェクトの一覧と、各フェーズの構成の形（工程あり／なし）を確認する。
6. **起点データが手段2（JSON 直接編集）で書き換えられている場合がある。** `changeLog` の最新
   エントリと実データが食い違っていても、実データを正として扱う。食い違いに気づいたら、
   今回追記する `changeLog` エントリの `summary` に「手編集分を取り込んだ」旨を1文添える。

#### 利用者が画面から書き出したファイルを持っている場合

「画面で直して書き出した」「書き出したファイルを取り込んで」と言われたとき。

1. **2つのファイルの場所を確認する**（`.html` と `.json` の対）。
   多くはブラウザのダウンロード場所にある。片方しか無い場合は、`.json` があれば足りる。
2. **`output/<meta.id>/` へ移す。**
   - **同じ名前のファイルが既にある場合は上書きしない。** 止めて確認する。
     画面の書き出しは版数を1つ上げているので、通常は衝突しない。衝突は取り違えの可能性が高い。
   - `.json` だけを移した場合は、その旨を伝える（対の `.html` が無い状態になる）。
3. **`output/<meta.id>/CHANGELOG.md` に1行足す。**
   画面からの書き出しはこのファイルを更新しないため、**放置すると履歴が飛ぶ。**
   修正指示の欄は「（画面上での手直し）」でよい。
4. **移した `.json` が、以後の起点になる。** 次の改版はこれを読む。
5. **その JSON の `changeLog` の最新エントリは「画面上での手直し」となっており、
   何を変えたかの明細を持たない。** 実データを正として扱い、`changeLog` から実データを戻さない。
6. **`-tmp-` を含む id があれば、正式な id に付け替える**（名称を英字化したもの）。
   付け替えた id は次の改版の `changeLog.changes[]` にすべて記録する。
   `-tmp-` が残っているのは想定どおりの状態であって、不備ではない。
7. 移した後の HTML の**絶対パスを伝える。**「こちらを開いてください」まで言う。
   移す前のファイルを開いたままだと、次の書き出しでそれまでの変更が消える。

**続けて修正の依頼が入っている場合は、4 の起点データからそのまま Step 2 へ進む。**
取り込みだけで一度切らない。

### Step 2. 指示の分類と差分の適用

| 分類 | 例 | 主な変更対象 |
|---|---|---|
| リスケジュール | 「実装を2週間後ろ倒し」 | タスクの日付、マイルストンの `date` |
| 要素の追加 | 「確認の工程を入れて」 | `tasks[]` 等に要素追加 |
| 要素の削除 | 「リハーサルは不要」 | 該当要素の削除 |
| 属性の変更 | 「担当を変えて」「名前を変えて」 | 該当フィールドのみ |
| 状態の更新 | 「設計は終わった」 | 該当タスクの `status`（`done` なら `progress` も100） |
| 前提の変更 | 「実装を3名体制に」 | `resources` を変え `durationDays` を再計算 |
| 構成の形の変更 | 「機能ごとにまとめ直して」 | 該当フェーズの配下を組み直す |
| プロジェクトの分割・統合 | 「勉強会は別にして」 | `projects[]` の組み直し |

#### 状態の更新（最も軽い改版）

「設計は終わった」「機能Aの実装に入った」のような依頼。**画面からは変えられないので、この経路が正になる。**

1. 対象のタスクを特定する。**工程名やマイルストン名で言われた場合は、その配下のタスクすべてが対象。**
   どのタスクを指しているか判断が付かない場合だけ聞き返す。
2. 該当タスクの `status` を書き換える。
   - `done` にした場合は、**そのタスクの `progress` を 100 にする。**
   - `inprogress` にした場合は `progress` に触らない。
3. **日程には一切触らない。** 開始日・終了日・所要日数・依存・バッファのどれも動かさない。
4. **親（マイルストン・工程・フェーズ・プロジェクト）の `status` は書かない。** 配下から算出される。
5. `meta` を更新する（`revision` を +1、`generatedAt`、`sourceRequest`、`previousRevisionFile`）。
   `calendar.today` を現在日にする。**タイムラインの範囲は変わらないので、休業日の追加は不要。**
6. `changeLog` に1件追記する。`changes[]` には変えたタスクを1件ずつ記録する。
7. Step 4（検証と出力）へ進む。

**報告は短くてよい。** 完了にしたタスクの名前と、新しい HTML の絶対パスがあれば足りる。
**全体の終了日は動かないので、動いていないことをわざわざ書かない。**

**予定外の日程変更を混ぜない。** 「終わった」という報告に対して、
残りの日程を勝手に詰めたり前倒ししたりしない。詰めるかどうかは別の依頼として確認する。

**「倍にして」「2週間延ばして」のような期間の指示の読み方:**

| 指示 | 何を動かすか |
|---|---|
| 「倍にして」「延ばして」「短くして」 | **`durationDays`（開始日〜終了日）** を動かす。日付を動かし、`durationDays` を実日数と一致させる |
| そのとき `effortDays` は | **作業量が増えた**（範囲が増えた・見積もりが甘かった）なら同じ比率で変える。**日程だけを動かす**（後ろ倒し、待ちが入った）なら変えない |
| どちらか判断できないとき | **作業量が増えた**として扱い、`meta.assumptions` に1行残す |

`effortDays` を変えても、`durationDays` は式で出し直さず**指示された期間を優先する**（指示が最も強い）。

**比率で決まるバッファは、改版で自動的に計算し直さない**（`20-scheduling-rules.md` 第5章）。
日程だけを動かしたときは据え置き、作業を足した・減らしたときだけその工程のバッファを計算し直す。

各分類の詳しい適用手順（依存の連動のさせ方、バッファの調整順、id の付け替えルールなど）は
`SPEC.md` 5-3 の「Step 2. 差分の適用」に一覧がある。**判断に迷ったら参照する。**
共通して守ること:

- 直前の版の JSON を起点に差分編集する。ゼロから作り直さない。
- 既存ファイルを上書きせず、新しい版として出力する。
- **変更していない要素の `id` は必ず維持する。**
- **プリセットは変えない。構成の形とプロジェクトの区切りは、明示的に指示されない限り変えない。**
- 何を変えたかを `changeLog` と `CHANGELOG.md` の両方に残す。

### Step 3. メタ情報の更新

1. `meta.revision` を+1。
2. `meta.generatedAt` を現在時刻に。
3. `meta.sourceRequest` を今回の修正指示の原文に差し替える。
4. `meta.previousRevisionFile` を、起点にした JSON のファイル名にする。
5. `meta.preset` は変えない。
6. **`meta.title` を組み直す**（A の Step 5-2 の規則をそのまま当てはめる。プロジェクト名を変えた／
   件数が変わった／`groupName` を変えた、のいずれかに当たったら必ず）。
7. `meta.assumptions` のうち今回の変更で無効になったものを除き、新たに置いた前提を追加する。
8. **`calendar` を更新する。**
   - `calendar.today` を現在日にする。
   - 日程が動いてタイムラインが伸縮したら `timelineStart` / `timelineEnd` を引き直す。
   - **新しい範囲に入った休業日を `holidays` に足す。** 年をまたいだ場合は、その年の分が
     `20-scheduling-rules.md` 第1章の一覧にあるかを先に確かめる。
   - **この更新は日程を計算する前に行う。** 後から足すと、休業日が稼働日として計算された日程が残る。
9. `changeLog` に1件追記する（既存のエントリは書き換えない）。`changes[]` には機械的に追える
   粒度で全変更を記録する（要約だけで済ませない）。
   `summary` は**その版でやったことだけ**。構成の数え上げを書かない。

### Step 4. 検証と出力

A の Step 6 とまったく同じ手順（`--out-root` で置き場を渡すことも含めて同じ）。
`meta.revision` を上げてあるので、新しい版のファイルとして出力される。

### Step 5. 報告

1. 新しい版の HTML と JSON の絶対パス
2. 前の版からの変更点（`changeLog.changes` を読みやすくしたもの）
3. 全体の終了日が変わったかどうか（変わった場合は変更前後の日付）
4. バッファの残量（0 になった場合はその旨を明示する）
5. 構成の形やプロジェクトの区切りを変えた場合は、変更前後の構成

---

## C. その他の直し方（利用者に案内する）

チャットでの修正指示（上記 B）のほかに、2つの直し方がある。利用者から「他にやり方はあるか」
「軽く直したい」「試しに動かしたい」と聞かれたら、次のように案内する。

| 手段 | どこで | 残るか | 向いていること |
|---|---|---|---|
| **JSON ファイルの直接編集** | エディタで `output/<meta.id>/` 内の最新版 `.json` を直接書き換える | 残る（版数は上げない） | 誤字修正、担当変更1件など、1か所で閉じる直し |
| **画面上の一時編集** | 生成した HTML を開いて、バーのドラッグ・行の追加削除など | 残らない（再読み込みで消える） | 動かしてみて確かめる。確定させたい場合は、編集の帯の「書き出す」で新しい JSON・HTML を出すか、そのまま本書 B の手順で AI に渡す |

**タスクのステータス（完了・進行中）は画面からも切り替えられる。** タスクの詳細パネルの「ステータス」プルダウンで直接変えられ、打ち消し線・バーの濃さ・上位への波及は連動する。ただしこれは「画面上の一時編集」と同じ扱いで、書き出すまでは残らない。**マイルストン以上は配下から自動で決まるため、画面からの切り替えは無い。** 確実に残したい場合、またはマイルストン以上を変えたい場合は本書 B（AI への修正指示）を使う。

迷ったときの目安: **直したい場所が2か所以上に波及するなら B（AI への修正指示）。1か所で
閉じるなら JSON 直接編集。まだ決める前の確認なら画面上の一時編集。**

**「今後こう作ってほしい」は、この3つではなく定義ファイルを直す。**
置き場の `definitions/` のファイルを直接書き換える（画面に定義ファイルを見る・直す口は無い）。
**直しても、いま開いている資料は変わらない。次に生成するものから効く**（`SPEC.md` 5-7 / 6-17）。

---

## D. 仕様についての質問

「画面で直したものはどこへ行くのか」「書き出したファイルをどうすればいいか」
「印刷はできるのか」のように、**このツールの仕様そのものを聞かれたとき。**

1. **`FAQ.md` を読んで答える。** 想定される質問と答えが一通り入っている。
2. `FAQ.md` に無い場合は、次の順で当たる。

   | 何を聞かれたか | どこを見るか |
   |---|---|
   | 直し方・修正依頼の出し方 | `HOW-TO-REVISE.md` |
   | 階層・日程・出力の決まり | `definitions/` 直下 |
   | 用途ごとの作業・見積もり・体制 | `definitions/presets/<採用したプリセット>/` |
   | データの形 | `schema/schedule.schema.json` |

3. **どこにも根拠が無いことは、推測で答えない。** 分からないと伝える。
   仕様を確かめずに答えると、利用者はそれを仕様として扱う。

**答えたあと、同じ質問が繰り返し出るようなら `FAQ.md` に足す。**

---

## 参照ファイル一覧

| 何を知りたいか | どこを見るか |
|---|---|
| 階層の定義、構成の形の選び方 | `definitions/10-hierarchy.md` |
| 稼働日、目標日と着手時期、間に合わないときの詰め方、バッファ、状態の書き方 | `definitions/20-scheduling-rules.md` |
| 識別子、出力先、改版規則、日付表記、`meta.notes` の基準 | `definitions/30-output-rules.md` |
| その組織のやり方（稼働、目標日への向き合い方、作業の束ね方、呼び方） | `definitions/50-user-context.md` |
| 用途ごとの標準作業・見積もり・体制 | `definitions/presets/<採用したプリセット>/` |
| データの形（正） | `schema/schedule.schema.json` |
| 動作するサンプル | `schema/example-schedule.json`（1プロジェクト）/ `schema/example-schedule-event.json`（2プロジェクト） |
| 検証の実装 | `templates/validate_schedule.py` |
| 埋め込み・出力の実装 | `templates/build.py` |
| **このツールの仕様についての質問への答え** | `FAQ.md` |
| **利用者向けの、生成した後の直し方** | `HOW-TO-REVISE.md` |
| **受け取った人が最初に読むもの** | `README.md` |
| **SKILL_ROOT / 置き場の決め方** | 本書 Step 1-0 |
| このスキルでは足りない判断の経緯・未決事項 | `SPEC.md`（同梱していない場合は利用者に確認する） |
