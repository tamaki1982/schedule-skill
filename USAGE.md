# 使い方の詳細（リポジトリの中身と置き場所）

`README.md` に入りきらない、リポジトリ構造まわりの詳細をここにまとめる。

---

## SKILL_ROOT と「置き場」

このスキルは2つの場所を区別して動く。

| 呼び方 | 何を指すか |
|---|---|
| **SKILL_ROOT** | このリポジトリ自体（`SKILL.md` や `templates/` がある場所。通常は `~/.claude/skills/schedule-gantt/`） |
| **置き場** | 生成物と自分用の定義ファイルを置く場所。SKILL_ROOT とは別の場所にあり、スキル本体の更新（`git pull`、または zip の入れ替え）や再配置の影響を受けない |

**このリポジトリ自体には生成物を置かない。** 生成したスケジュール（HTML・JSON）と、自分用に
書き換えた定義ファイルは、導入時に決めた「置き場」（このリポジトリの外）に入る。

置き場は、初めてスケジュールを作るときに Claude Code が候補を1つ提示し、確認を取ったうえで決まる。
決めた場所はプロジェクトの `CLAUDE.md`（無ければグローバルの `CLAUDE.md`）に記録され、次回以降は
聞かれない。

**正しく置けたかの確かめ方**: 置き場のフォルダの中に `definitions/`（自分用にコピーされたもの）が
できていれば設定は済んでいる。まだスケジュールを1本も作っていなければ `output/` はまだ無い
（最初の1本を作ったときにできる）。

---

## フォルダの中身

### このリポジトリ（SKILL_ROOT）

| 場所 | 何が入っているか | 書き換えるか |
|---|---|---|
| `SKILL.md` | AI が読む作り方の指示書 | 書き換えない |
| `definitions/` | 判断基準の**配布時点の既定値**。階層・日程・出力の決まりと、用途ごとのプリセット | **書き換えない。** 自分用に書き換えるのは置き場にコピーされた方 |
| `schema/` | データの形と、動作するサンプル2本 | 書き換えない |
| `templates/` | 出力の器（HTML テンプレート）と、検証・書き出しの処理（`build.py` / `validate_schedule.py`） | 書き換えない |
| `HOW-TO-REVISE.md` | 作った後の直し方 | — |
| `FAQ.md` | よくある質問 | — |

### 置き場（このリポジトリの外）

| 場所 | 何が入っているか | 書き換えるか |
|---|---|---|
| `<置き場>/definitions/` | 上の `definitions/` のコピー。**自分用に書き換えるのはこちら** | 書き換える |
| `<置き場>/output/` | 作ったスケジュール（HTML・JSON・CHANGELOG.md） | ここに成果物が増える |

---

## 困ったときは

**このツールの仕様についての質問は、そのまま AI に聞けばよい。** `FAQ.md` に答えを用意してあり、
AI はそれを読んで答える。

よくある質問の例:

- 画面で直した内容はどこへ行くのか
- 書き出したファイルをどうすればいいか
- 目標日に間に合わないときはどうなるのか
- 印刷はできるのか

`FAQ.md` に無い場合は `SKILL.md` の「参照ファイル一覧」の表に従って `definitions/` 内の該当ファイルを
確認する。それでも根拠が無いことは、推測せずに「分からない」と答える。
