# 使い方の詳細（リポジトリの中身と置き場所）

`README.md` に入りきらない、リポジトリ構造まわりの詳細をここにまとめる。

---

## SKILL_ROOT と「置き場」

このスキルは2つの場所を区別して動く。

| 呼び方 | 何を指すか |
|---|---|
| **SKILL_ROOT** | このリポジトリ自体（`SKILL.md` や `templates/` がある場所。通常は `~/.claude/skills/schedule-gantt/`） |
| **置き場** | 生成物と、**自分で書き換えた定義ファイルだけ**を置く場所。SKILL_ROOT とは別の場所にあり、スキル本体の更新（`git pull`、または zip の入れ替え）や再配置の影響を受けない |

**このリポジトリ自体には生成物を置かない。** 生成したスケジュール（HTML・JSON）と、
自分で書き換えた定義ファイルは、導入時に決めた「置き場」（このリポジトリの外）に入る。

置き場は、初めてスケジュールを作るときに Claude Code が候補を1つ提示し、確認を取ったうえで決まる。
決めた場所はプロジェクトの `CLAUDE.md`（無ければグローバルの `CLAUDE.md`）に記録され、次回以降は
聞かれない。

**正しく置けたかの確かめ方**: `CLAUDE.md` に `## schedule スキルの生成物置き場所` の行があり、
そこに書かれたフォルダが実在すれば設定は済んでいる。
**置き場の中は、最初は空でよい。** `output/` は最初の1本を作ったときにでき、
`definitions/` は**定義ファイルを実際に書き換えたときに初めてできる。**

---

## 定義ファイルは2か所にある

`definitions/` だけは、SKILL_ROOT と置き場の両方にありうる。**役割が違う。**

| 場所 | 何が入るか |
|---|---|
| `<SKILL_ROOT>/definitions/` | **配布された既定値。全ファイルが揃っている。** 更新のたびに丸ごと入れ替わる |
| `<置き場>/definitions/` | **自分で書き換えたファイルだけ。** 無いことも多く、それが普通の状態 |

**読むときの決まりは1行。**

> **ファイル1本ごとに、置き場に同じ相対パスのファイルがあればそれを読み、無ければ SKILL_ROOT の既定値を読む。**

判定は**フォルダ単位ではなくファイル単位**なので、あるプリセットの `30-estimation.md` だけを
書き換えた場合、**そのファイルだけが自分のものに差し替わり、同じプリセットの残り5ファイルは
最新の既定値が使われる。**

### なぜこの形か

置き場は「書き換えたものを更新から守る」ための場所である。
**書き換えていないファイルまで置き場に持つと、そのファイルも更新から守られてしまい、
こちらが直した分が永久に届かない。**

置き場に持つのは、守る必要があるファイルだけでよい。

### 書き換え方と、戻し方

| やりたいこと | どうするか |
|---|---|
| 定義を書き換えたい | **AI に頼むのが早い。** 直すべき1ファイルを選び、SKILL_ROOT から置き場へ写して、そこを直す。**一式はコピーしない** |
| 書き換えたファイルを既定値に戻したい | **置き場の `definitions/` からそのファイルを消す。** 次から最新の既定値が使われる |
| 新しい用途（プリセット）を足したい | 置き場側に新しいフォルダを作る（`definitions/90-how-to-add-preset.md`）。**既定のプリセットを土台にする場合もフォルダ名を変える。** 元のプリセットは既定のまま残り、更新を受け取り続ける |

書き換えたファイルには、見出しの直後に**どの版から取ったかの1行**が入る。
生成のたびにこの行と現在の既定値の版が突き合わされ、**離れていれば知らされる**（生成は止まらない）。

### 古い形からの移行

**版 4.3 までは、初回に定義ファイル一式が置き場へコピーされていた。**
このコピーは更新されないため、**こちらが直した分が1つも届かない状態になっていた。**

置き場に一式が残っている場合、次にスケジュールを作るときに1回だけ移行が走る。

1. 置き場の `definitions/` が `definitions-old-<日付>/` に**退避される**（消されない）。
2. **退避したコピーに書いてある定義バージョンが読まれる。**
   そのコピーは「その版の時点の既定値」なので、**比べる相手もその版に合わせる。**
3. 既定値と中身が同じファイル、自分で足したプリセット、
   **そして「その版から今までに配布側が直したファイル」は、自動で仕分けられる。**
4. **残ったものだけが一覧で示され、「自分で書き換えたものはあるか」を1回聞かれる。**
   心当たりが無ければ「無い」と答えてよい。
   一覧には**その版以降に配布側が何を変えたか**が添えられるので、「これは自分ではない」と判断できる。
5. 答えたものだけが置き場に戻り、残りは最新の既定値が使われる。
   **戻さなかったファイルも `definitions-old-<日付>/` に残っている**ので、後から取り出せる。

**定義を一度も書き換えていない場合は、3 で全部が片付き、何も聞かれずに終わる。**
これが普通の結果である。**身に覚えのないファイルが並ぶようなら、それは配布側の作りが悪い。**

何を戻し、何を戻さなかったかは、移行の直後に必ず知らされる。
**戻さなかったものは2つに分けて示される。**

| どちらか | 示され方 |
|---|---|
| **触っていないことが確定したもの** | **件数と、その間にこちらが何を変えたかだけ**が伝えられる。確定しているので、心当たりは聞かれない |
| **確定まではできなかったもの** | ファイル名が並ぶ。そこに心当たりがあれば、そう言えば退避先から戻せる |

**独自のプリセットを足していた場合**、そのフォルダは丸ごと戻る。
ただし `00-index.md` の「プリセット一覧」の表に自分で足していた1行は引き継がれない。
**選択肢は `presets/` 直下のフォルダを見て決まるので、一覧に無くても選べなくなることはない**
（そもそも索引に行を足さないことを勧めている。`definitions/90-how-to-add-preset.md` Step 4）。

---

## フォルダの中身

### このリポジトリ（SKILL_ROOT）

| 場所 | 何が入っているか | 書き換えるか |
|---|---|---|
| `SKILL.md` | AI が読む作り方の指示書 | 書き換えない |
| `definitions/` | 判断基準の**配布時点の既定値**。階層・日程・出力の決まりと、用途ごとのプリセット | **書き換えない**（更新で丸ごと入れ替わる）。書き換えは置き場側に写してから |
| `schema/` | データの形と、動作するサンプル2本 | 書き換えない |
| `templates/` | 出力の器（HTML テンプレート）と、検証・書き出しの処理（`build.py` / `validate_schedule.py`） | 書き換えない |
| `HOW-TO-REVISE.md` | 作った後の直し方 | — |
| `FAQ.md` | よくある質問 | — |

### 置き場（このリポジトリの外）

| 場所 | 何が入っているか | 書き換えるか |
|---|---|---|
| `<置き場>/definitions/` | **自分で書き換えたファイルだけ。** 無いのが初期状態 | 書き換えたものがここに増える |
| `<置き場>/output/` | 作ったスケジュール（HTML・JSON・CHANGELOG.md） | ここに成果物が増える |

---

## 困ったときは

**このツールの仕様についての質問は、そのまま AI に聞けばよい。** `FAQ.md` に答えを用意してあり、
AI はそれを読んで答える。

よくある質問の例:

- 画面で直した内容はどこへ行くのか
- 書き出したファイルをどうすればいいか
- 目標日に間に合わないときはどうなるのか
- 印刷はできるのか

`FAQ.md` に無い場合は `SKILL.md` の「参照ファイル一覧」の表に従って `definitions/` 内の該当ファイルを
確認する。それでも根拠が無いことは、推測せずに「分からない」と答える。
